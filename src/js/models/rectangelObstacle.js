export default class RectangelObstacle {
    speed
    xPos
    yPos
    width
    height

    constructor(speed, xPos, yPos, width, height) {
        this.speed = speed
        this.xPos = xPos
        this.yPos = yPos
        this.width = width
        this.height = height
    }
}