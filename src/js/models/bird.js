export default class Bird {
    xCenter
    yCenter
    radius
    speed

    constructor(speed, xCenter, yCenter, radius) {
        this.speed = speed;
        this.xCenter = xCenter
        this.yCenter = yCenter
        this.radius = radius
    }
}